{% if items or empty %}
  {%- if items -%}
    <div class="os-widgets-taxonomy-display-type-menu">
    {%- for item in items -%}
      <div class="os-widgets-taxonomy-display-type-menu-item">{{ item.value }}</div>
    {%- endfor -%}
    </div>
  {%- else -%}
    {{- empty -}}
  {%- endif -%}
{%- endif %}
